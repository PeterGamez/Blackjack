Put topup package images here.

Naming convention:
- Use token count as filename.
- Example: 100.png, 500.png, 1000.png

The Topup page falls back to:
/topup-packages/{tokens}.png

If a package has a custom image path from backend, that path is still used.
